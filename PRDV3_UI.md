# 🎛️ 前端 UI 开发需求规格说明书（针对 PRD v3.0）

> ⚠️ **本文档已废弃 — 2026-07-27**
> UI 规格已全部整合至 `PRDV3.1.md`。后续所有 UI 需求以 `PRDV3.1.md` 为准，请勿参考本文档。
> 归档原因：双文档维护导致信息源分裂，PRD v3.1 统一为单一真值源。

> **目标读者**：React / Vue 前端开发工程师、UI/UX 设计师、WebGL 图形工程师
> **核心原则**：UI 层 **只负责渲染与交互收集**，**绝对禁止**进行任何 DSP 数学运算（如 ZDF 系数计算、FFT 或传递函数公式推导）。所有物理数据由 C++ 侧通过 JSI 二进制通道推送。

---

## 1. 技术栈与性能红线

| 项目 | 约束 |
|------|------|
| **框架** | React 18+ 或 Vue 3+（推荐 React，便于与 JUCE JSI 集成） |
| **图形引擎** | **必须**使用 WebGL 2.0 或 Canvas 2D（硬件加速开启），禁止使用 DOM 元素绘制曲线 |
| **状态管理** | Zustand / Redux Toolkit 或 Pinia（需支持与 JSI 回调的松耦合） |
| **包体积红线** | 前端构建产物（JS + CSS + 字体 + 静态资源）**总大小 ≤ 2MB** |
| **帧率目标** | 界面交互及曲线动画 **稳定 60fps**，UI 线程 CPU 占用率需接近 0%（依赖 GPU 渲染） |
| **视觉风格** | 深色专业主题（对标 Pro-Q 3 的磨砂玻璃/极简暗黑质感），支持高 DPI（Retina）适配 |

---

## 2. 主视图（EQ 曲线编辑区域）

这是 UI 的**核心画布**，所有频段节点的增删改查在此区域完成。

### 2.1 画布坐标系与映射
- **X 轴（频率）**：对数刻度，范围 **20Hz ~ 20kHz**。
- **Y 轴（增益）**：线性刻度，范围 **-30dB ~ +30dB**（视觉显示范围，实际 DSP 支持 ±60dB，但视图裁剪至此区间）。
- **网格背景**：绘制半透明栅格线（建议 X 轴为十倍频程标记：20, 50, 100, 200, 500, 1k, 2k, 5k, 10k, 20k；Y 轴每 6dB 或 12dB 一条）。

### 2.2 曲线渲染（物理响应曲线）
- **数据来源**：C++ 通过 `Float32Array` 推送 **≤ 150 个极值/拐点坐标**（频率, 增益）。
- **渲染管线**：
  1. 接收点阵后，使用 **Catmull-Rom 样条插值** 补全为连续平滑路径（像素级精度）。
  2. 叠加绘制所有活动频段的总和曲线（主 EQ 曲线）。
  3. 
  4. 若有选中的单个频段，可半透明高亮显示该频段的独立贡献曲线（可选）。
- **⚠️ 绝对禁令**：前端**禁止**通过传递函数公式自行计算曲线，只负责插值与描边。

### 2.3 视图缩放与平移（交互控制）
- **缩放**：鼠标滚轮滚动的瞬间，以当前光标位置为中心，**缩放 X 轴（频域）**，范围锁定在 20Hz ~ 20kHz。
- **平移**：按住鼠标中键或空格键+拖拽，左右平移频率视图。
- **联动逻辑**：每当缩放或平移停止（或实时触发），UI 必须**立即向 C++ 侧发送当前视图的频率边界（`viewMinFreq`, `viewMaxFreq`）**，请求 C++ 重新计算该区间内的 150 个极值点并回传。

---

## 3. 频段节点（Band）交互逻辑

### 3.1 节点类型与视觉标识
支持四种节点类型，UI 需用不同颜色或图标区分：
- **Bell（钟形）**：默认，标准山丘/山谷形状。
- **Low/High Shelf（搁架）**：左右两侧趋于平坦。
- **Low/High Cut（高通/低通）**：曲线末端垂直下落。

### 3.2 节点的增删改查操作
- **增加节点**：
  - **手动**：双击画布空白处，在该频率位置生成一个 **Gain = 0dB、Q = 1.0** 的默认 Bell 节点。
  - **频谱抓取（重点）**：用户从频谱分析仪区域**拖拽**至画布释放时，新节点的 `Gain` 初始值 = 释放点对应的**实时频谱幅值（dBFS）**，频率 = 释放点 X 轴频率。此操作生成的节点默认选中。
- **移动节点（改）**：鼠标拖拽节点控制点（圆形手柄）：
  - **上下拖拽**：改变 **Gain（增益）**。
  - **左右拖拽**：改变 **Frequency（频率）**。
  - **按住 Shift + 左右拖拽**：改变 **Q 值（带宽）**。
- **删除节点**：选中节点后按 `Delete` / `Backspace` 键，或双击节点弹出删除选项。
- **选中节点**：单击节点手柄高亮显示（增加外发光或放大尺寸）。

### 3.3 节点上限与防呆设计
- **硬性上限**：**最多 24 个节点**。
- UI 行为：当节点数 = 24 时，双击画布和频谱抓取功能**必须被禁用**，并给予微弱的 Toast 提示（如“已达到最大频段数 24”）。

### 3.4 复制/粘贴频段（Copy/Paste Band）
- 右键点击任意节点弹出上下文菜单，包含 **“复制频段”**。
- 在画布空白处右键，若剪贴板有频段数据，显示 **“粘贴频段”**（粘贴后的 Gain 默认继承原值，频率偏移 +10% 以防止完全重叠）。

### 3.5 频段独奏（Band Solo）与能量匹配
- 点击节点上的独奏按钮（小耳机图标）或双击频段标签，触发 Solo。
- **视觉反馈**：Solo 状态下，未选中的其他曲线变暗（Opacity 降低至 20%）。
- **自动增益匹配**：触发 Solo 时，UI 无需计算，由 C++ 侧自动执行 RMS 能量匹配。UI 只需显示一个 **“Solo 激活”** 状态指示灯。

---

## 4. 频谱分析仪（实时背景频谱）

### 4.1 视觉呈现
- 位于 EQ 曲线画布的**底层**（作为背景层），半透明颜色（通常为蓝/青渐变或冷灰色）。
- 展示实时输入信号（或处理后信号，取决于用户设置）的幅频分布。

### 4.2 数据流与平滑（UI 侧职责）
- **接收数据**：C++ 侧已对 FFT 数据进行了 EMA（100ms 时间常数）平滑，并经过三重缓冲推送到 UI。
- **UI 职责**：UI 仅将接收到的频谱数组（频率 bins）映射到画布 X/Y 坐标并绘制填充多边形（或柱状图）。
- **瀑布图（可选进阶）**：若要求瀑布图，需维护一个长度固定的历史帧数组（如 30 帧），用于绘制 3D 拖尾效果。

### 4.3 智能休眠机制（UI 生命周期管理）
- **监听窗口可见性**：UI 必须监听 `document.visibilityState` 或 JUCE 的 `onClose` 事件。
- **休眠触发**：当 UI 标签页隐藏或插件窗口关闭时，**必须通过 JSI 向 C++ 发送一个原子标志位（`setAnalyzerActive(false)`）**，通知 C++ 侧停止 FFT 计算与推流。
- **唤醒**：窗口重新可见时，发送 `setAnalyzerActive(true)`，恢复频谱渲染。

---

## 5. 右侧/底部控制面板（参数控件）

### 5.1 动态均衡（Dynamic EQ）控件
当选中一个频段节点时，面板显示：
- **模式切换**：`Compression`（压缩） / `Expansion`（扩展） 单选按钮。
- **阈值（Threshold）**：滑块/数字框，范围 -60dB ~ 0dB。
- **比率（Ratio）**：滑块 1:1 ~ 20:1。
- **Attack / Release / Hold**：时间滑块（ms 或秒）。
- **侧链源（Sidechain）**：下拉菜单选择 `Internal（内部RMS）` 或 `External（DAW侧链）`。

### 5.2 全局通道路由（L/R & M/S）
- **模式切换**：`Stereo（立体声）` / `Left（仅左）` / `Right（仅右）` / `Mid（中）` / `Side（侧）`。
- **链接（Link）**：Toggle 开关，开启后左右/中侧通道的增益调整联动。

### 5.3 过采样（Oversampling）
- 下拉选择：`Off` / `2x` / `4x`。
- **切换保护**：切换时 UI 无需特殊处理，由 C++ 侧内部执行 50ms 交叉淡入淡出。

### 5.4 输出增益与旁路（Bypass）
- **输出增益**：数字旋钮/滑块，范围 **±24dB**，显示精确到 0.1dB。
- **旁路开关**：大按钮，按下时：
  - 视觉上整体 EQ 曲线变灰（或隐藏）。
  - **响度补偿**：由 C++ 计算并补偿 RMS，UI 只需显示补偿激活指示灯（如 "Loudness Comp" 高亮）。

### 5.5 侧链监听（Sidechain Listen）
- 按钮控件，激活时 UI 可无特殊视觉变化，由 C++ 切换监听路由。UI 在按钮旁边显示一个小指示灯（绿/红）。

---

## 6. 预设与全局管理（Menu / Top Bar）

### 6.1 预设加载与保存
- **预设列表**：下拉菜单显示内置预设（如 "Default", "Vocal Cut", "Mastering Glue"）。
- **导入/导出**：
  - **导出为文件**：触发下载 `.eqpreset`（JSON）文件。
  - **复制为文本（重要）**：按钮点击后，将当前预设编码为 **Base64 字符串**并自动写入系统剪贴板。UI 需集成 `navigator.clipboard.writeText()`。
  - **从文本粘贴**：输入框或粘贴按钮，解码 Base64 并应用预设。

### 6.2 撤销 / 重做（Undo / Redo）
- 快捷键支持：`Cmd/Ctrl + Z`（撤销），`Cmd/Ctrl + Shift + Z`（重做）。
- UI 侧维护一个**指令快照栈**（存储操作前后的频段状态快照），点击撤销时发送对应的逆操作 JSI 指令给 C++ Helper 线程。

---

## 7. JSI 通信契约（前端视角）

### 7.1 接收数据（Audio -> UI）
- **通道名**：`"EQ_Curve_Data"` 和 `"Spectrum_Data"`。
- **数据格式**：`Float32Array`（非 JSON）。
  - 曲线数据排列：`[freq1, gain1, freq2, gain2, ..., freqN, gainN]`（共 300 个浮点数，即 150 点）。
  - 频谱数据排列：`[bin1_amp, bin2_amp, ..., binM_amp]`（长度取决于 FFT 尺寸 / 2，约 4096 个浮点）。
- **前端处理**：接收到数据后，**必须立即通过 `requestAnimationFrame`** 触发重绘，不得阻塞主线程。

### 7.2 发送数据（UI -> Audio）
- **通道名**：`"Parameter_Change"`。
- **频率节流（重点）**：UI 在拖拽滑块或节点时，触发的 JSI 调用频率**必须由前端主动节流（Throttle）**，确保相邻两次发送间隔 **≥ 1ms**（例如使用 `lodash.throttle` 或自定义时间戳滤波），丢弃中间冗余值。
- **数据格式**：`Float32Array` 或简单的结构化克隆（如 `[bandIndex, paramType, value]`）。严禁使用 JSON 字符串传递高频连续变化值。

### 7.3 结构性指令（UI -> Helper）
- 对于"新建节点"、"删除节点"、"切换滤波器类型"等操作，可以使用 **JSON 对象**（因为频率低，不涉及性能瓶颈），通过 `postMessage` 发送至 Helper 线程。

---

## 8. 性能优化与构建检查清单

- [ ] **WebGL 渲染**：曲线绘制使用 `WebGL2RenderingContext` 或 `OffscreenCanvas`，避免 CPU 软渲染。
- [ ] **内存管理**：频谱历史帧数组（瀑布图）长度固定，使用环形缓冲区，避免无限增长。
- [ ] **Tree-shaking**：构建配置（Vite/Webpack）必须开启极度压缩，剥离 `console.log` 及开发依赖，确保最终 JS Bundle < 1.5MB，CSS < 0.5MB。
- [ ] **高 DPI 适配**：使用 `window.devicePixelRatio` 缩放 Canvas 分辨率，保证 Retina 屏清晰。
- [ ] **UI 休眠验证**：必须提供开发者模式下的日志开关，验证窗口关闭时 `setAnalyzerActive(false)` 已被调用。

---

## 9. 交付物要求（UI 开发阶段）

1. **Figma / 设计稿**：标注颜色变量（深色主题）、字体（建议 Inter 或系统无衬线）、控件尺寸（Hit Area 至少 44x44px 以便手指操作）。
2. **组件库**：封装可复用的通用组件（旋钮、滑块、Toggle 开关、下拉菜单），支持键盘导航（Tab 键切换焦点）。
3. **模拟数据适配器（Mock Adapter）**：在 C++ JSI 接口尚未就绪时，提供一套 `MockJSI` 类，模拟发送 `Float32Array` 随机曲线及频谱数据，确保 UI 渲染逻辑可独立测试。