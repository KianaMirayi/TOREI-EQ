# PRD v3.0 审查报告

> 审查日期：2026-07-27
> 审查范围：`PRDV3.md` + `PRDV3_UI.md`
> 结论：**架构方向正确，v1.0 范围需裁剪，存在 3 个结构性风险需在设计阶段解决**

---

## 1. 总体评估

两份文档作为技术愿景质量很高——C++/WebUI 混合架构选型合理，JSI 通信协议设计成熟，性能约束具体可量化。核心问题在于 v1.0 范围过大，以及 AAX 双 UI 策略、Undo/Redo 位置、FFT 线程归属三个结构性决策需要修正。

---

## 2. 结构性问题（🔴 必须在编码前解决）

### 2.1 AAX 双 UI 维护被低估

> PRD §6："AAX 版本强制剥离 WebView，降级为纯原生 OpenGL 绘制"

**问题**：这不是降级，是独立维护第二套 UI 渲染路径。Web UI（React + WebGL）和原生 OpenGL 之间的曲线渲染、节点交互、频谱绘制逻辑完全无法复用。真实成本 = 2x UI 开发 + 永久 2x UI bug 修复。

**Pro-Q 3 参考**：FabFilter 自己也没走这条路——他们的 UI 是自研跨平台矢量渲染引擎，不是双套代码。

**推荐方案（三选一）**：

| 方案 | 描述 | 风险 |
|------|------|------|
| **A** | AAX 也内嵌 WebView2（近年 Avid 审核已放宽，多个商业插件已通过） | 低 — 需要验证最新 Avid 审核政策 |
| **B** | 用 JUCE `Component::paint()` 体系画 UI（不用 OpenGL），WebView 和原生共用相同的 JUCE 渲染接口 | 中 — 需要抽象一层渲染后端 |
| **C** | v1.0 只做 VST3/AU，AAX 延后到 v1.2 | 低 — 务实做法 |

**建议：先选 C，v1.0 站稳后再评估 A 或 B。**

### 2.2 Undo/Redo 存在"双源头真值"风险

> PRD_UI §6.2："UI 侧维护指令快照栈，通过 JSI 发送逆操作指令"

**问题**：C++ Helper 线程是参数的真值源（Single Source of Truth）。UI 侧的快照栈是"派生状态"，当 JSI 消息丢失或乱序时：

```
用户拖拽节点 → UI 快照A → JSI #1 丢失 → C++ 状态仍是B
用户拖拽节点 → UI 快照B → JSI #2 到达 → C++ 状态变为A
用户 Ctrl+Z  →  UI 认为撤销到快照A，发送逆操作 → C++ 状态混乱
```

**修正方案**：

- Undo/Redo 栈 **必须在 C++ Helper 线程维护**（它持有真值源）
- UI 只发送"触发撤销/重做"指令（`"Undo"` / `"Redo"` 字符串）
- C++ 执行状态回滚后，将**完整参数快照**推回 UI 做渲染更新
- UI 端的 `Cmd+Z` 快捷键只负责转发触发指令

### 2.3 FFT 计算线程归属不明确

> PRD §3.2："FFT 数据在 C++ 后台线程进行 EMA 平滑处理后推送"
> PRD §5.1："严禁在 `processBlock` 内调用超越函数"

**问题**：两处描述之间存在空白——FFT 的加窗、复数运算、幅值转换具体在哪个线程执行？如果在 `processBlock`，8192 点 FFT 在 32-sample buffer 下完全不可行；如果不在，数据传输路径未定义。

**修正建议**：在 PRD §3.2 补充：

> FFT 计算全链路（加窗 → FFT → 幅值转换 → EMA 平滑 → Triple Buffer 写入）均在独立的高优先级 `std::thread` 中执行。`processBlock` 仅负责将当前音频帧拷贝至无锁环形缓冲区（SPSC Queue），严禁在音频线程执行任何 FFT 相关运算。

---

## 3. 技术可行性问题（🟡 需要设计阶段细化）

### 3.1 "禁止一切超越函数"过于绝对

> PRD §5.1："严禁在 `processBlock` 内调用 `std::sin`, `std::cos`, `std::tan` 或 `std::exp`。"

**理解意图**：避免非确定性性能尖峰。但在实际 DSP 中：

- ZDF 滤波器系数更新**仅在用户调参时触发**（频率 << 1Hz），此时调用一次 `std::tan` 完全安全
- LUT + 插值的误差在低频区（<100Hz）可能比直接计算更差
- 过于严格会导致过度设计（为偶尔调一次的参数维护大型 LUT）

**建议改为**：

> `processBlock` 内**每采样点/每帧执行**的热路径严禁超越函数。系数更新路径（仅参数变更时触发）不受此限，但应确保不在 `processBlock` 临界区内调用。

### 3.2 150 点重算缺少触发节流

> 附录二 §2："缩放或平移视图后，前端应触发 C++ 重算点阵。"

**问题**：用户拖拽平移时，鼠标每移动 1px 触发一次 `viewMinFreq`/`viewMaxFreq` 更新。不加节流的话，C++ 每秒被轰炸 60+ 次频率响应重算请求。

**修正建议**：

> UI 侧对 `viewMinFreq`/`viewMaxFreq` 推送增加 **50ms 防抖（debounce）**。平移/缩放进行中，使用现有点阵做线性视口裁剪渲染（不请求重算）；交互停止 50ms 后触发一次重算。

### 3.3 Spectrum Grab 频域/幅值映射精度

> 附录二 §1："Gain 初始值 = 鼠标释放位置对应的实时频谱幅值（dBFS）"

**实现细节需要明确**：鼠标释放时取的是**单个频率 bin 的值**还是**周围 bins 的插值**？8192 点 FFT 在 20Hz 附近每个 bin 约 5.4Hz 宽——直接取 bin 值会造成低频区 ±3dB 以上的抓取误差。

**建议**：取释放频率 ±0.5 octave 范围内的 **RMS 均值**或**三次样条插值**。

---

## 4. 功能缺口（🟠 对标 Pro-Q 3 的决策项）

以下为 Pro-Q 3 具备但 PRD 未覆盖的功能。每个都需要决策：v1.0 加入 / 延后 / 不做。

| 功能 | 实现成本 | 用户感知度 | 建议 |
|------|----------|-----------|------|
| **Phase Display**（相位响应曲线） | 中 | 高 — Mixed/Natural Phase 必须有 | ✅ v1.0 加入 |
| **Auto-Gain**（自动增益补偿） | 低 | 极高 — 跨预设对比的核心功能 | ✅ v1.0 加入 |
| **Notch Filter**（陷波滤波器） | 低 — Bell 的超高 Q 变体 | 中 | ✅ v1.0 加入 |
| **频段 Mute**（独立频段静音） | 低 — Solo 的对称实现 | 中 | ✅ v1.0 加入 |
| **键盘精细调节**（Ctrl/Cmd+拖拽） | 低 | 高 — 所有参数精确输入 | ✅ v1.0 加入 |
| **频谱 Pre/Post 切换** | 低 — 切换数据源 | 中 | ✅ v1.0 加入 |
| **Tilt Shelf**（倾斜搁架） | 低 — Shelf 变体 | 低 | 🔶 v1.1 |
| **MIDI Learn** | 中 | 中 — 合成器场景重要 | 🔶 v1.1 |
| **EQ Match**（频谱匹配） | 高 | 中 — 竞品差异化功能 | 🔶 v1.2 |
| **Spectrum Freeze/Tilt/Speed** | 低 | 低 — 锦上添花 | ⬜ v1.2 |
| **瀑布图（Waterfall）** | 中 | 低 | ⬜ v1.2 |
| **无障碍（a11y）** | 中 | 合规需要 | ⬜ v1.2 |

### 未覆盖的 UX 规格

| 缺失项 | 说明 |
|--------|------|
| **频段类型切换交互** | 如何从 Bell 变成 Shelf/Cut？右键菜单？节点旁下拉？未定义 |
| **双击空白创建 vs 双击节点删除** | Pro-Q 3 是双击空白=创建 Bell，双击节点=删除。PRD 只描述了前者 |
| **Q 值拖拽的视觉反馈** | PRD 说 Shift+左右拖拽=改 Q，但 Pro-Q 3 会显示带宽环——PRD 未提及 |
| **输出增益的计量显示** | 只说 ±24dB，没说峰值表/削波指示灯 |

---

## 5. 架构亮点（🟢 保持不动）

以下设计决策质量很高，应在实现中严格保留：

- **§4.1 三重缓冲 + Float32Array 零拷贝**：成熟的高性能跨线程数据流模式
- **§4.1 UI→Audio 节流 ≥1ms**：精准的防止 UI 回调洪水策略
- **§4.2 `isNonRealtime()` 跳过 WebView**：非破坏性 offline bounce 的关键防护
- **§4.3 PDC 突变 + 2048 samples Mute Fill**：DAW 兼容性的正确做法
- **§5.2 Smart Bypass**：50ms Crossfade + Loudness Compensation 比大多数商业插件做得都好
- **§6 体积红线 2MB + Win7 降级**：分发侧考虑周全
- **Catmull-Rom 样条 + ≤150 极值点**：精度与传输成本的最优平衡点
- **附录二全部 7 条**：每一条都是实际联调中会踩的坑，已经提前标注

---

## 6. v1.0 范围裁剪建议

当前 PRD 的 Must-Have 包含：ZDF + FIR 线性相位 + 混合相位 + Dynamic EQ + Sidechain。如果全做，联调周期至少 4-6 个月而非 PRD 估算的 3-4 周。

### 建议 v1.0（核心静态 EQ）

```
✅ ZDF 引擎（Bell / Shelf / Cut / Notch）
✅ 频谱分析仪（FFT + 三重缓冲 + 休眠）
✅ 曲线渲染（Catmull-Rom + 150 点）
✅ UI 完整交互（节点 CRUD、拖拽、缩放平移）
✅ Smart Bypass + 响度补偿
✅ 过采样 2x/4x
✅ 通道路由 L/R & M/S
✅ 预设系统（JSON + Base64 分享）
✅ Undo/Redo（C++ 侧）
✅ Phase Display + Auto-Gain
✅ 频段 Solo + Mute
✅ L/R 独立处理
```

### 延后到 v1.1

```
🔶 Dynamic EQ（Envelope Follower + Threshold/Ratio/Attack/Release）
🔶 Sidechain 输入
🔶 FIR 线性相位模式
🔶 混合相位模式
🔶 MIDI Learn
🔶 Tilt Shelf
```

### 延后到 v1.2+

```
⬜ AAX 格式
⬜ EQ Match
⬜ 瀑布图
⬜ 无障碍化
```

**理由**：静态 EQ 是一个完整可交付产品。Dynamic EQ + 三种相位模式 + Sidechain 的组合会让调试矩阵爆炸（每种相位模式 × 每种滤波器类型 × 动态/静态 = N×M×2 种状态组合）。在稳定的静态 EQ 基座上迭代动态功能是更安全的路径。

---

## 7. 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| WebView2 在部分 DAW 中崩溃 | 中 | 高 | Win7 降级逻辑已考虑；增加 Win8/10 早期版本的 WebView2 缺失检测 |
| JSI 通信延迟导致 UI 拖拽卡顿 | 中 | 中 | 1ms 节流 + Catmull-Rom 视觉插值已缓解；增加丢帧容错 |
| ZDF LUT 精度不足导致高频失真 | 低 | 高 | 用 JUCE 内置 `dsp::IIR::Coefficients` 验证对比；必要时回退为直接计算 |
| 多实例 CPU 占用过高 | 中 | 中 | FFT 休眠机制已考虑；增加实例数 >4 时自动降采样 FFT 分辨率 |
| DAW 自动化包络与节流冲突 | 低 | 高 | 压力测试已列入 §7；自动化参数走独立通道（不节流） |

---

## 8. 审查结论

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | ⭐⭐⭐⭐⭐ | C++ DSP + WebUI 分离清晰，通信协议成熟 |
| 性能约束 | ⭐⭐⭐⭐ | 量化具体，3 处需细化 |
| 功能完整度 | ⭐⭐⭐ | 缺少 10+ 对标项，需决策取舍 |
| 可实施性 | ⭐⭐⭐ | v1.0 范围过大，建议裁剪 |
| 风险识别 | ⭐⭐⭐⭐ | 测试指南覆盖主要风险，缺线程归属 |

**总体**：PRD v3.0 可以作为开发起点，但建议在编码前完成三项修正：①裁剪 v1.0 范围 ②将 Undo/Redo 移至 C++ ③明确 FFT 线程归属。修正后的 PRD v3.1 可直接进入架构实现阶段。

---

*审查人：Hermes Agent (deepseek-v4-pro)*
*下次审查建议：修正项落地后，针对实现方案做第二轮技术审查*
