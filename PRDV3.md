# 🎛️ 现代化高阶数字均衡器插件（对标 Pro-Q 3）- 产品需求文档 (PRD) v3.0

## 1. 项目概述与核心愿景

本产品是一款面向专业混音与母带工程师的高性能数字均衡器（EQ）插件。产品在视觉与交互体验上全面对标行业标杆 **FabFilter Pro-Q 3**。通过“C++ 极致 DSP 引擎 + 系统原生 Web UI”的混合架构，在提供 0 延迟、工业级纯净音质的同时，实现跨时代丝滑的现代化动态视觉交互。

---

## 2. 核心功能需求 (Must-Have)

### 2.1 动态均衡处理 (Dynamic EQ)
*   **触发机制**：支持内部（RMS/Peak 检测）及外部 DAW 侧链（Sidechain）输入触发。
*   **动态模式**：必须同时支持“向下压缩（Downward Compression）”与“向上扩展（Upward Expansion）”。
*   **时域控制**：提供可调的 Attack / Release / Hold 时间。
*   **⚠️ 架构铁律**：包络跟随（Envelope Follower）与增益计算逻辑必须 **100% 在 C++ 音频线程内完成**，严禁交由 UI 侧计算或受限于 UI 刷新率。

### 2.2 DSP 音频处理多模式引擎
*   **IIR ZDF 模式 (默认)**：基于零延迟反馈（ZDF）与拓扑保持变换（TPT），消除高频频率扭曲。
*   **FIR 线性相位模式 (高级)**：支持线性相位，避免频段相位失真。
    *   **精准延迟档位**：强制量化为 Low (256 samples)、Medium (1024 samples)、Maximum (8192 samples)。
*   **混合相位模式 (Mixed/Natural Phase)**：高阶 IIR 匹配幅频 + 全通滤波器相位补偿。相位补偿量限制在最小相位偏移的 60% 以内，避免前回声 (Pre-ringing)。

### 2.3 通道路由与全局信号控制
*   **通道模式 (L/R & M/S)**：支持独立处理左/右声道或中/侧 (Mid/Side) 通道，支持通道间 Solo 与 Link 功能。
*   **过采样 (Oversampling)**：内置 2x / 4x 可选倍率。必须附带高质量的线性相位抗混叠滤波器。
*   **输出增益控制**：主界面右下角提供 ±24dB 数字输出增益旋钮，用于最终电平补偿。

---

## 3. 灵魂交互与视觉渲染

### 3.1 物理曲线与视图交互
*   **曲线数据传输与前端插值协议**：
    *   C++ 侧计算真实幅频响应时，**仅提取极值点与拐点（总数 ≤ 150 点）**，通过 `Float32Array` 发送。
    *   前端 Canvas/WebGL 接收后，强制使用 **Catmull-Rom 样条插值**完成像素级渲染。此设计既保证了物理真实性，又极大减少了跨线程数据吞吐。
*   **频谱抓取 (Spectrum Grab)**：支持用户从频谱分析仪显示区域直接拖拽鼠标创建 EQ 节点。UI 将鼠标横纵坐标映射为频率 (Hz) 与增益 (dB)，通过 JSI 发送至 Helper 线程快速生成新 Bell 滤波器。
*   **视图控制**：支持鼠标滚轮缩放频域（20Hz-20kHz）与拖拽平移。

### 3.2 高动态频谱分析仪 (Analyzer)
*   **声学视觉精度规格**：FFT 算法固定采用 **Blackman-Harris 窗**，FFT 尺寸定为 **8192**，重叠率 (Overlap) 固定为 **75%**，确保低频（20-100Hz）具备极高的视觉分辨率。
*   **后台平滑推流**：FFT 数据在 C++ 后台线程进行指数移动平均（EMA，约 100ms）平滑处理后推送。
*   **CPU 智能休眠机制**：当 WebView 窗口隐藏 (`OnClose`) 或插件界面不可见时，C++ 音频线程必须通过原子布尔标志位**停止 FFT 数据采集与推流**，仅保留 EQ 主算法，严控多实例下的系统资源消耗。

### 3.3 频段独奏 (Band Solo) 与参数管理
*   **精准独奏**：单击或长按频段节点单独监听，采用 **RMS 能量匹配算法** 维持听感音量平滑。
*   **频段复用与预设流转**：
    *   支持右键点击频段节点进行“复制/粘贴频段 (Copy/Paste Band)”。
    *   预设系统除 XML/JSON 外，**支持“复制预设为文本”（Base64 编码）**，以便于用户在论坛或工单中直接粘贴共享。
*   **Undo / Redo 栈**：在 UI 侧维护指令快照栈，通过 JSI 发送逆操作指令。

---

## 4. 关键架构设计与跨线程生命线

### 4.1 跨线程通信机制 (JSI 二进制规范)
*   **高频推流 (Audio -> UI)**：采用 **三重缓冲 (Triple Buffering)** 机制推送平滑后的 FFT 数据。
*   **低延迟交互 (UI -> Audio) 与参数节流 (Throttling)**：
    *   高频参数传递（如拖拽节点）必须采用 `Float32Array` 零拷贝传输。
    *   **节流保护**：UI -> Audio 方向的参数更新必须在 Helper 线程增加时间戳滤波器，**丢弃间隔 < 1ms 的重复指令**，绝对防止高频 UI 回调堵塞音频线程。

### 4.2 UI 生命周期与无头扫描规避 (Headless Scan)
1.  **无头规避**：`isNonRealtime()` 为 `true` 时，强制跳过 WebView 初始化。
2.  **异步预热**：`prepareToPlay` 仅异步预读资源，推迟实例创建至首次打开界面。
3.  **安全关闭**：UI 关闭时隐藏窗口并**强制销毁 JavaScript 上下文**，释放内存。

### 4.3 延迟补偿 (PDC) 突变预警与安全切换
*   当切换 ZDF / 线性相位 / 混合相位模式，且涉及 `setLatencySamples` (延迟采样点) 变化时：
    *   插件应自动向 DAW 请求重启流 (`RequestRestart`)。
    *   若 DAW 不支持，需在内部执行 **2048 个采样点的静音填充 (Mute Fill)**，防止轨道整体时序发生突兀偏移或爆音。

---

## 5. DSP 算法与实时线程硬核纪律

### 5.1 音频线程 (processBlock) 性能铁律
*   **禁止动态分配**：严禁任何 `new`, `delete`, `malloc`。
*   **⚠️ 数学计算禁令**：**严禁在 `processBlock` 内调用 `std::sin`, `std::cos`, `std::tan` 或 `std::exp`**。高频系数更新必须使用预扭曲 BLT 的 **查表法 (Look-up Table)** 或低阶多项式近似。

### 5.2 智能旁路 (Smart Bypass) 与响度匹配
*   **无损交叉淡入淡出**：旁路开启/关闭时，强制执行 **50ms 的 Crossfade**。
*   **响度补偿 (Loudness Compensation)**：旁路切换时，强制启用响度补偿逻辑，计算 EQ 带来的整体 RMS 增益变化并进行反向抵消，确保开关 EQ 对比时**不产生听觉响度突跳**，提供最客观的混音对比环境。

---

## 6. 编译、分发与运行环境约束

*   **引擎绑定**：严禁内嵌 CEF。macOS 使用 WKWebView，Windows 使用 WebView2。
*   **体积红线**：前端静态资产总大小限制在 **2MB** 以内。
*   **优雅降级**：Win7/8 缺失 WebView2 时降级至 JUCE 原生静态图形绘制。
*   **AAX 专供版**：AAX 版本强制剥离 WebView，降级为纯原生 OpenGL 绘制，以通过 Avid 严格的图形上下文审查。

---

## 7. 开发排期与高风险测试指南

*   **联调深水区 (建议预留 3-4 周)**：重点攻克 ZDF 查表法精度 + 三重缓冲 + JSI 二进制通信节流机制。
*   **压力测试基准**：
    1.  **极端自动化测试**：画满方波级别的 Gain/Freq 自动化包络，验证 ZDF 拓扑与 Throttling 拦截器。
    2.  **极限缓冲测试**：32 samples 极小音频缓冲区下运行，监测锁竞争。
    3.  **UI 休眠验证**：关闭 UI 界面，使用性能分析工具确认 FFT 线程 100% 处于挂起状态。


【附录二：执行层交互与数据规格终审补遗】

1. 频谱抓取（Spectrum Grab）增益映射：
   从频谱分析仪拖拽生成的 Bell 节点，其初始 Gain 值必须等于鼠标释放位置所对应的实时频谱幅值（dBFS），而非固定 0dB。

2. 曲线点阵视图联动规范：
   C++ 侧发送的 150 个极值点必须基于当前 UI 缩放窗口（频率起始/截止范围）进行对数均匀采样。缩放或平移视图后，前端应触发 C++ 重算点阵。

3. 多频段视觉融合规则：
   当多个 Bell 节点的频率距离 < 1.5 × 带宽（Q 值决定）时，UI 曲线应渲染为视觉上的合并形态，但 DSP 侧保持独立滤波器结构不变。

4. 动态 EQ 侧链监听功能：
   必须提供“侧链监听（Sidechain Listen）”按钮。激活时，监听输出切换为原始侧链输入信号（预处理前），用于调试触发源。

5. 过采样切换抗爆音机制：
   过采样倍率（Off / 2x / 4x）切换时，必须执行 50ms 交叉淡入淡出或 512 sample 延迟激活，防止滤波器状态突变导致爆音。

6. 预设版本管理：
   所有预设 JSON/XML 文件必须强制包含 `"version": "1.0.0"` 字段。软件升级后读取旧预设时，需依据版本号执行字段迁移逻辑，禁止静默丢弃参数。

7. 频段数量上限：
   系统最大支持 24 个 EQ 频段节点（与 Pro-Q 3 对齐）。UI 应在达到上限时禁用新增按钮并给出提示。