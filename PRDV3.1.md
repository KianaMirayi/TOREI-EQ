# 🎛️ 现代化高阶数字均衡器插件（对标 Pro-Q 3）- 产品需求文档 (PRD) v3.1

> 基于 PRD v3.0 + 审查报告（PRDV3_Review.md）整合修订
> 修订日期：2026-07-27
> 核心变更：v1.0 范围裁剪、Undo/Redo 移至 C++、FFT 线程归属明确、AAX 策略调整
> 
> ⚠️ **文档合并通知**：本文件已整合原 `PRDV3_UI.md` 的全部 UI 规格内容。`PRDV3_UI.md` 已废弃，后续所有 UI 需求以本文为准。


## 1. 项目概述与核心愿景

本产品是一款面向专业混音与母带工程师的高性能数字均衡器（EQ）插件。产品在视觉与交互体验上全面对标行业标杆 **FabFilter Pro-Q 3**。通过“C++ 极致 DSP 引擎 + 系统原生 Web UI”的混合架构，在提供 0 延迟、工业级纯净音质的同时，实现跨时代丝滑的现代化动态视觉交互。

**v1.0 发布目标**：交付一款功能完整、性能稳定的静态 EQ 插件（含频谱分析仪、完整节点交互、通道路由、过采样、预设管理）。动态 EQ、线性相位模式、混合相位模式及 Sidechain 作为 v1.1 增量发布。


## 2. v1.0 功能范围（Must-Have）

| 模块 | 包含功能 | 状态 |
|------|---------|------|
| **DSP 引擎** | IIR ZDF（Bell / Shelf / Cut / Notch） | ✅ v1.0 |
| **频谱分析仪** | FFT + 三重缓冲推送 + 智能休眠 | ✅ v1.0 |
| **曲线渲染** | C++ 150 极值点 + Catmull-Rom 插值 | ✅ v1.0 |
| **节点交互** | 增删改查、拖拽（Gain/Freq/Q）、24 节点上限 | ✅ v1.0 |
| **视图控制** | 缩放（滚轮）、平移（中键拖拽） | ✅ v1.0 |
| **频段独奏/静音** | Solo + Mute，RMS 能量匹配 | ✅ v1.0 |
| **Smart Bypass** | 50ms Crossfade + 响度补偿 | ✅ v1.0 |
| **过采样** | 2x / 4x，线性相位抗混叠 | ✅ v1.0 |
| **通道路由** | L/R & M/S，Link 功能 | ✅ v1.0 |
| **预设管理** | JSON 序列化 + Base64 文本分享 | ✅ v1.0 |
| **Undo/Redo** | C++ 侧维护（详见 §4.4） | ✅ v1.0 |
| **Phase Display** | 相位响应曲线叠加显示 | ✅ v1.0 |
| **Auto-Gain** | 自动增益补偿（跨预设对比） | ✅ v1.0 |
| **输出增益** | ±24dB 数字旋钮 | ✅ v1.0 |
| **频谱 Pre/Post 切换** | 分析仪信号源切换 | ✅ v1.0 |
| **键盘精细调节** | Ctrl/Cmd+拖拽 精细步进 | ✅ v1.0 |
| **频段复制/粘贴** | 右键菜单 Copy/Paste Band | ✅ v1.0 |
| **频谱抓取** | 从分析仪拖拽创建节点 | ✅ v1.0 |

### v1.1 延后功能

| 模块 | 包含功能 | 目标版本 |
|------|---------|----------|
| **动态 EQ** | Envelope Follower + Threshold/Ratio/Attack/Release | v1.1 |
| **Sidechain** | 外部 DAW 侧链输入触发 | v1.1 |
| **FIR 线性相位** | 256/1024/8192 三档延迟 | v1.1 |
| **混合相位** | IIR + 全通相位补偿（60% 约束） | v1.1 |
| **Tilt Shelf** | 倾斜搁架滤波器 | v1.1 |
| **MIDI Learn** | 参数 MIDI 映射 | v1.1 |

### v1.2+ 延后功能

| 模块 | 目标版本 |
|------|----------|
| AAX 格式支持 | v1.2 |
| EQ Match（频谱匹配） | v1.2 |
| 瀑布图（Waterfall） | v1.2 |
| 无障碍化（a11y） | v1.2 |


## 3. 核心功能需求（v1.0 详细规格）

### 3.1 DSP 音频处理引擎（静态 EQ）

- **IIR ZDF 模式（默认）**：基于零延迟反馈（ZDF）与拓扑保持变换（TPT），消除高频频率扭曲。支持滤波器类型：
  - **Bell（钟形）**：标准峰值/谷值滤波
  - **Low/High Shelf（搁架）**：低频/高频搁架
  - **Low/High Cut（高通/低通）**：斜率 12/24/48/96 dB/Oct
  - **Notch（陷波）**：超高 Q 值 Bell 变体
- **过采样（Oversampling）**：内置 Off / 2x / 4x 可选倍率，附带线性相位抗混叠滤波器。切换时执行 **50ms 交叉淡入淡出** 防爆音。
- **通道模式**：支持 Stereo / Left / Right / Mid / Side 独立处理，Link 开关控制通道联动。
- **输出增益**：±24dB 数字旋钮，步进 0.1dB。
  - **峰值表 + 削波指示灯**：旋钮旁显示实时输出峰值电平（数字条 + 峰值数值），超过 -0.1dBFS 时点亮红色削波指示灯。数据来源：C++ 侧每音频帧计算 `Float32 maxPeak`，经 JSI 推送 UI 渲染。
- **Auto-Gain**：自动计算并补偿 EQ 引入的整体 RMS 增益变化，确保预设切换/旁路对比时响度一致。

### 3.2 动态均衡处理（Dynamic EQ）- v1.1

> ⚠️ 以下为 v1.1 规格，v1.0 开发阶段保留设计预留

- **触发机制**：支持内部（RMS/Peak 检测）及外部 DAW 侧链输入触发。
- **动态模式**：向下压缩（Downward Compression）与向上扩展（Upward Expansion）。
- **时域控制**：可调的 Attack / Release / Hold 时间。
- **架构铁律**：包络跟随（Envelope Follower）与增益计算逻辑必须 **100% 在 C++ 音频线程内完成**，严禁交由 UI 侧计算。

### 3.3 FIR 线性相位 & 混合相位 - v1.1

> ⚠️ 以下为 v1.1 规格，v1.0 仅做架构预留

- **FIR 线性相位**：
  - 精准延迟档位：Low（256 samples）、Medium（1024 samples）、Maximum（8192 samples）
  - 分块卷积算法（Partitioned Convolution / Overlap-Save）
- **混合相位（Mixed/Natural Phase）**：
  - 高阶 IIR 匹配幅频 + 全通滤波器相位补偿
  - 相位补偿量限制在最小相位偏移的 **60%** 以内，暴露为 UI 权重滑块


## 4. 交互与视觉渲染（v1.0）

### 4.1 主视图（EQ 曲线编辑区域）

- **画布坐标系**：
  - X 轴（频率）：对数刻度，20Hz ~ 20kHz
  - Y 轴（增益）：线性刻度，-30dB ~ +30dB（视觉显示范围）
  - 网格背景：半透明栅格线（X 轴十倍频程标记，Y 轴每 6dB/12dB）
- **曲线渲染协议**：
  - C++ 侧计算真实幅频响应，**仅提取极值点与拐点（总数 ≤ 150 点）**，通过 `Float32Array` 发送
  - 前端 Canvas/WebGL 接收后，强制使用 **Catmull-Rom 样条插值** 完成像素级渲染
  - **绝对禁令**：前端禁止通过传递函数公式自行计算曲线
- **视图缩放与平移**：
  - 滚轮缩放 X 轴（频域），范围锁定 20Hz ~ 20kHz
  - 中键/空格+拖拽平移
  - **联动逻辑**：平移/缩放停止后 **50ms 防抖（Debounce）**，触发 C++ 重算当前视图区间内的 150 个极值点

### 4.2 频段节点交互

- **节点类型**：Bell / Low Shelf / High Shelf / Low Cut / High Cut / Notch
- **类型切换**：右键点击节点 → 上下文菜单选择类型，或节点旁下拉控件
- **增加节点**：
  - 手动：**双击画布空白处**，生成 Gain=0dB、Q=1.0 的默认 Bell 节点
  - 频谱抓取：从频谱分析仪区域拖拽至画布释放，Gain 初始值 = 释放点对应的实时频谱幅值（**取 ±0.5 octave 范围内 RMS 均值**），频率 = 释放点 X 轴频率
- **移动节点**（拖拽控制点）：
  - 上下拖拽 → Gain
  - 左右拖拽 → Frequency
  - Shift + 左右拖拽 → Q 值（显示带宽环视觉反馈）
  - Ctrl/Cmd + 拖拽 → 精细步进模式（1/10 精度）
- **删除节点**：选中后按 Delete / Backspace，或**双击节点本身**（与双击空白创建形成对称交互）
- **节点上限**：硬性上限 **24 个**，达到时禁用新增并 Toast 提示
- **频段复制/粘贴**：右键菜单 Copy/Paste Band，粘贴后频率偏移 +10% 防止重叠
- **频段独奏（Solo）**：点击节点耳机图标，未选中曲线变暗（Opacity 20%），C++ 自动执行 RMS 能量匹配
- **频段静音（Mute）**：Solo 的对称功能，独立静音单个频段

### 4.3 频谱分析仪（Analyzer）

- **视觉呈现**：位于 EQ 曲线底层，半透明蓝/青渐变，展示实时输入信号幅频分布
- **Pre/Post 切换**：支持分析仪信号源在输入信号（Pre）与处理后信号（Post）间切换
- **FFT 声学规格**：
  - 窗函数：**Blackman-Harris**
  - FFT 尺寸：**8192**
  - 重叠率（Overlap）：**75%**
- **FFT 线程归属（关键架构决策）**：
  - FFT 计算全链路（加窗 → FFT → 幅值转换 → EMA 平滑 → Triple Buffer 写入）**均在独立的高优先级 `std::thread` 中执行**
  - `processBlock` 仅负责将当前音频帧拷贝至无锁环形缓冲区（SPSC Queue）
  - **严禁在音频线程执行任何 FFT 相关运算**
- **后台平滑推流**：EMA 时间常数 **100ms**，平滑后经三重缓冲推至 UI
- **智能休眠机制**：
  - 窗口隐藏或插件界面不可见时，UI 通过 JSI 发送 `setAnalyzerActive(false)`
  - C++ 侧停止 FFT 采集与推流，仅保留 EQ 主算法
  - 恢复可见时发送 `setAnalyzerActive(true)`
- **峰值电平推流（输出计量）**：
  - C++ 侧每音频帧计算输出 `Float32 maxPeak`，与 FFT 频谱数据共用推送通道
  - UI 接收到后更新输出增益旁的数字峰值表 + 削波指示灯（≥ -0.1dBFS 变红）

### 4.4 Phase Display（相位响应曲线）

- 在 EQ 曲线画布上以**叠加方式**显示当前信号链的相位响应曲线
- 坐标系：X 轴频率（对数），Y 轴相位（度，范围 -180° ~ +180°）
- 颜色区别于 EQ 增益曲线（建议使用亮绿色或橙色半透明）
- 数据来源：C++ 侧计算相位响应极值点（≤ 150 点），通过独立 `Float32Array` 通道推送


## 5. 控制面板（参数控件）

### 5.1 频段参数面板（选中节点时显示）

- 频率（Frequency）：数字旋钮/输入框，步进 1Hz（低频）/ 10Hz（高频）
- 增益（Gain）：数字旋钮/输入框，步进 0.1dB
- Q 值（Q/Bandwidth）：数字旋钮/输入框
- 滤波器类型下拉菜单（Bell/Shelf/Cut/Notch）
- Solo / Mute 按钮
- 斜率（Slope）：仅 Cut/Shelf 显示，12/24/48/96 dB/Oct

### 5.2 全局控制面板

- **通道路由**：模式切换 Stereo / Left / Right / Mid / Side，Link Toggle
- **过采样**：下拉选择 Off / 2x / 4x
- **输出增益**：数字旋钮 ±24dB，步进 0.1dB
- **旁路（Bypass）**：大按钮，启用时 EQ 曲线变灰，响度补偿自动激活
- **Auto-Gain**：Toggle 开关
- **频谱 Pre/Post**：Toggle 切换分析仪信号源

### 5.3 侧链监听（Sidechain Listen）- v1.1

> v1.1 功能，v1.0 预留 UI 位置


## 6. 预设与全局管理

### 6.1 预设操作

- 下拉菜单加载内置预设（Default / Vocal Cut / Mastering Glue 等）
- **导出为文件**：下载 `.eqpreset`（JSON）文件
- **复制为文本**：当前预设编码为 Base64 字符串，自动写入剪贴板
- **从文本粘贴**：解码 Base64 并应用预设
- **预设版本管理**：所有 JSON 预设强制包含 `"version": "1.0.0"` 顶级字段

### 6.2 Undo / Redo（关键架构决策）

- ⚠️ **Undo/Redo 栈必须在 C++ Helper 线程维护**（C++ 持有参数真值源）
- UI 只发送触发指令（`"Undo"` / `"Redo"` 字符串）
- C++ 执行状态回滚后，将完整参数快照推回 UI 渲染更新
- 快捷键：`Cmd/Ctrl + Z`（撤销），`Cmd/Ctrl + Shift + Z`（重做）


## 7. 架构设计与跨线程通信

### 7.1 跨线程通信机制（JSI 二进制规范）

- **高频推流（Audio → UI）**：
  - 三重缓冲（Triple Buffering）推送平滑后的 FFT 数据及曲线极值点
  - 数据格式：`Float32Array`（非 JSON）
  - 曲线数据排列：`[freq1, gain1, ..., freqN, gainN]`（N ≤ 150）
- **低延迟交互（UI → Audio）**：
  - 高频参数传递（拖拽节点）采用 `Float32Array` 零拷贝传输
  - **前端主动节流（Throttle）**：相邻两次发送间隔 **≥ 1ms**，丢弃中间冗余值
  - **⚠️ 例外**：DAW 自动化包络写入不受节流限制，走独立通道
- **结构性指令（UI → Helper）**：
  - 新建/删除节点、切换滤波器类型等低频操作使用 JSON 对象传递
  - 发送至非实时 Helper 线程处理

### 7.2 UI 生命周期管理

1. **无头规避**：`isNonRealtime()` 为 `true` 时，强制跳过 WebView 初始化
2. **异步预热**：`prepareToPlay` 仅异步预读资源，推迟 BrowserComponent 实例创建至首次打开界面
3. **安全关闭**：UI 关闭时隐藏窗口，强制销毁 JavaScript 上下文释放内存

### 7.3 延迟补偿（PDC）突变预警 - v1.1

> v1.1 实现，v1.0（ZDF 仅 0 延迟）无需处理


## 8. DSP 算法与实时线程纪律

### 8.1 音频线程（processBlock）性能铁律

- **禁止动态分配**：严禁 `new` / `delete` / `malloc`，警惕 `std::vector::push_back` 扩容
- **超越函数约束（细化）**：
  - `processBlock` 内**每采样点/每帧执行**的热路径严禁调用 `std::sin`/`cos`/`tan`/`exp`
  - **系数更新路径（仅用户调参时触发，频率 << 1Hz）不受此限**，允许直接计算以确保精度
  - 高频场景（如调制）使用 LUT + 线性插值

### 8.2 智能旁路（Smart Bypass）

- 旁路开启/关闭时强制执行 **50ms 交叉淡入淡出**
- **响度补偿（Loudness Compensation）**：C++ 计算 EQ 整体 RMS 增益变化并反向抵消，确保 A/B 对比无响度突跳

### 8.3 FFT 线程归属（明确）

- FFT 全链路在**独立高优先级 `std::thread`** 中执行
- `processBlock` 仅拷贝音频帧至 SPSC 无锁队列
- 音频线程与 FFT 线程通过原子操作同步，禁止互斥锁


## 9. 编译、分发与运行环境约束

### 9.1 包体积与引擎依赖

- **系统原生 WebView**：严禁内嵌 CEF
  - macOS：WKWebView
  - Windows：WebView2
- **体积红线**：前端静态资产总大小 ≤ **2MB**

### 9.2 多平台策略

| 格式 | UI 策略 | 分发状态 |
|------|---------|----------|
| **VST3（Win/macOS）** | 完整 WebView | ✅ v1.0 |
| **AU（macOS）** | 完整 WebView | ✅ v1.0 |
| **AAX（Pro Tools）** | **延后至 v1.2**，届时评估 WebView2 审核可行性或原生降级 | ⬜ v1.2 |
| **Standalone** | 完整 WebView | ✅ v1.0 |

- **优雅降级**：Win7/8 缺失 WebView2 时降级至 JUCE 原生静态图形绘制


## 10. 开发排期与高风险测试指南

### 10.1 联调深水区（建议预留 3-4 周）

重点攻克：
- ZDF 系数更新（系数路径允许超越函数，热路径 LUT）
- 三重缓冲 + JSI 二进制通信 + 前端 1ms 节流联调
- 150 点极值点提取 + Catmull-Rom 插值渲染精度验证

### 10.2 压力测试基准

1.  **极端自动化测试**：DAW 中方波级 Gain/Freq 自动化包络，验证 ZDF 状态稳定性与节流拦截器
2.  **极限缓冲测试**：32 samples 极小缓冲区下运行，排查锁竞争与 XRUN
3.  **UI 休眠验证**：关闭 UI，性能工具确认 FFT 线程 100% 挂起
4.  **多实例测试**：≥8 个插件实例同时运行，验证 CPU 与内存无泄漏


## 11. 附录：工程进阶约束（审查报告采纳项）

1.  **频谱抓取增益映射精度**：从频谱分析仪拖拽生成节点时，取释放频率 ±0.5 octave 范围内的 **RMS 均值** 作为初始 Gain 值。
2.  **视图联动防抖**：缩放/平移触发的 C++ 曲线重算必须带 **50ms 防抖（Debounce）**，交互进行中使用现有点阵线性裁剪渲染。
3.  **多频段视觉融合规则**：当多个 Bell 节点频率距离 < 1.5 × 带宽时，UI 曲线渲染为视觉合并形态，DSP 侧保持独立滤波器结构。
4.  **过采样切换抗爆音**：Off / 2x / 4x 切换时执行 **50ms 交叉淡入淡出**。
5.  **预设版本管理**：JSON 预设强制包含 `"version"` 字段，升级时执行字段迁移逻辑。
6.  **频段数量上限**：最大 24 个节点。
7.  **Undo/Redo 架构（已纳入 §6.2）**：C++ Helper 线程维护栈，UI 仅转发触发指令。
8.  **FFT 线程归属（已纳入 §8.3）**：独立线程执行，音频线程仅负责数据拷贝。
9.  **AAX 策略（已纳入 §9.2）**：延后至 v1.2。
10. **超越函数约束细化（已纳入 §8.1）**：热路径禁止，系数更新路径允许。


## 12. 审查结论与签署

| 维度 | 状态 |
|------|------|
| 架构设计 | ✅ 通过 |
| 性能约束 | ✅ 通过（细化后） |
| 功能范围 | ✅ v1.0 已裁剪，可执行 |
| 风险识别 | ✅ 已覆盖 |
| 可实施性 | ✅ 可进入架构实现阶段 |

**PRD v3.1 终审通过，可进入 JUCE C++ + React 双线并行开发阶段。**

---

*文档版本：v3.1*
*生效日期：2026-07-27*
*下次审查触发条件：v1.0 开发完成、进入 v1.1 规划前*